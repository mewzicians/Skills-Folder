interface:
  display_name: "Game QA Cleanup"
  short_description: "Reconcile and synchronize project truth"
  default_prompt: "Use $game-qa-cleanup to audit and synchronize the game project guidance, decisions, handoffs, mirrors, manifests, skills, and packages without changing gameplay."
